import MainContainer from "@/components/container/mainContainer/MainContainer";

export default function page() {
  return (
    <MainContainer className="w-full h-[calc(100%-3rem)] flex"></MainContainer>
  );
}
